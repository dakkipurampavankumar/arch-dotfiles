# Swayidle - Auto-lock and display power saving
# Background processes continue running when display is off
exec swayidle -w \
    timeout 900 'swaylock -f -c 24273a' \
    timeout 900 'swaymsg "output * power off"' \
    resume 'swaymsg "output * power on"'
