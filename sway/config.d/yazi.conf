# 1. Define the rules for the floating window
for_window [app_id="yazi-float"] floating enable, move position center,resize set height 700, resize set width 1200, move scratchpad, scratchpad show

# 2. Launch Yazi
bindsym $mod+e exec alacritty --class yazi-float -e yazi
